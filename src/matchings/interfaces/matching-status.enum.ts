export enum MatchingStatus {
  APPLIED = 'APPLIED', // 매칭 조회3
  FAILED = 'FAILED', // 매칭 조회4
  MATCHED = 'MATCHED', // 매칭 조회5
  ACCEPTED = 'ACCEPTED', // 매칭 조회6
  SUCCEEDED = 'SUCCEEDED', // 매칭 조회7
  REFUSED = 'REFUSED', // 매칭 조회8
  NOT_RESPONDED = 'NOT_RESPONDED', // 매칭 조회12
}
